package org.apache.zookeeper.server.cnxn.embedded;

import java.net.InetSocketAddress;

interface ECONST {

    int PORT = -1;
    String HOST = "localhost";

    InetSocketAddress LOCALHOST = new InetSocketAddress(-1);
}
